#include "main.h"
/**
*_isupper - check for uppercase character
*@c: variable declared
*Return: 0 or 1.
*/
int _isupper(int c)
{
int i;
if (c == i)
{
for (i = 65; i < 90; i++)
return (1);
else
return (0);
}
}
