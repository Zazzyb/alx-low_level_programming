#include "main.h"
/**
*_puts - prints a string follow by stdout in a new line
*@str: parameter to print
*Return: Always 0
*/
void _puts(char *str)
{
while (*str != '\0')
{
_putchar(*str++);
}
_putchar('\n');
}
